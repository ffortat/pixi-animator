TODO
X - change style of active reset buttons
X - Add frame around sprite in viewport (survol ou permanent)

X - sprite interpolation (dynamic)
		- add indicator on which frame change (i grey ?)
	- interpolation (%age ?) --> génère courbe
		- linear
		- acceleration
		- deceleration
		- square (?)

	- Add particles preview
	- Add particle textures listing & preview (UI)
	- Add record mode
		- if disabled : change default values
		- if enables : change values for a given frame

	- Save as
	- rename pivot to offset

	- fix pivot tool => apply rotation to coordinate change ?

TOFIX
	- prevent creation of indicator on currentframe when moving an indicator
	- remove indicators from old compositor when creating new one