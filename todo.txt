TODO
	- Tools (toolbox)
		- Move indicator in timeline
		- shortcut next/previous frame
	- set cursors for tools (arrows for move, crosshair for move pivot)
	
	- Rework pivot / position (pivot should follow position)
	- rename pivot to offset

	- sprite interpolation (dynamic)
		- add indicator on which frame change (i grey ?)
	- interpolation (%age ?) --> génère courbe
		- linear
		- acceleration
		- deceleration
		- square (?)

	- Add particles preview
	- Add particle textures listing & preview (UI)
	- Add record mode
		- if disabled : change default values
		- if enables : change values for a given frame

	- Add frame around sprite in viewport (survol ou permanent)

	- Save as